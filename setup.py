from setuptools import setup, find_packages


setup(
    name='cloudstorage',
    version='0.1mcash',
    author="Google",
    description="Cloudstorage api",
    packages=['cloudstorage'],
    include_package_data=True,
    install_requires=[
    ],
)
